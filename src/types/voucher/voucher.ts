export interface Voucher {
    id: number;
    name: string;
    code: string;
    discountPercent: number;
    maxDiscount: number;
    quantity: number;
    description: string;
    startDate: string;
    endDate: string;
    createdAt: string;
    updatedAt: string;
  }
  
  // Props cho component
